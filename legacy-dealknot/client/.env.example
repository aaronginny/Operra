# Copy to client/.env for local dev (or set in your hosting environment).
# When unset, the area input on every form falls back to free-text — the broker can still type any city/area.
VITE_GOOGLE_PLACES_API_KEY=
